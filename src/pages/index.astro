---
import SiteLayout from '../layouts/SiteLayout.astro';
const posts = (await Astro.glob('/src/pages/blog/*.md'))
  .sort((a, b) => new Date(b.frontmatter.pubDate) - new Date(a.frontmatter.pubDate))
  .slice(0, 5);
---
<SiteLayout title="Book Notes — Audiobooks & Reviews" description="Short, honest book notes and audiobook-friendly summaries.">
  <section style="border:1px solid #e5e7eb;border-radius:14px;padding:18px;background:#f8fafc">
    <h2 style="margin:0 0 8px 0">Welcome 👋</h2>
    <p style="margin:0 0 12px 0">A clean, fast, SEO-friendly site for English book notes and audiobook reviews.</p>
    <p style="margin:0">New here? Start from the latest posts, or browse the full <a href="/blog">Blog</a>.</p>
  </section>

  <h2>Latest posts</h2>
  <ul>
    {posts.map((p) => (
      <li>
        <a href={p.url}>{p.frontmatter.title}</a>
        {' — '}<span style="color:#6b7280">{new Date(p.frontmatter.pubDate).toDateString()}</span>
      </li>
    ))}
  </ul>
</SiteLayout>
